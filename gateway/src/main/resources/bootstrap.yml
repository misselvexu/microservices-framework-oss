spring:
  application:
    name: oss-gateway
  cloud:
    ## nacos config
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    ## enable discovery
    gateway:
      discovery:
        locator:
          enabled: true


nacos:
  server-addr: 127.0.0.1:8848